import React, {ReactNode} from "react";

import './ModalFooterRight.css'

interface Props {
	children: ReactNode,
}

export const ModalFooterRight: React.FC<Props> = ({}) => {

	return (
		<></>
	)
}
