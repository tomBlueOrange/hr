import React, {ReactNode} from "react";

import './DrawerFooterLeft.css'

interface Props {
	children: ReactNode,
}

export const DrawerFooterLeft: React.FC<Props> = ({}) => {

	return (
		<></>
	)
}
